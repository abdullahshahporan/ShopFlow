spring.application.name=SWE_Project

# ?? Database (PostgreSQL / NeonDB) ??
# Require SSL mode by default; production Neon URL should still be supplied via SPRING_DATASOURCE_URL.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-young-pine-a12twdvz-pooler.ap-southeast-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_HGUS7Rbd8mXu&sslmode=require&channelBinding=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_HGUS7Rbd8mXu}
spring.datasource.driver-class-name=org.postgresql.Driver

# ?? JPA / Hibernate ??
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# ?? Thymeleaf ??
spring.thymeleaf.cache=false

# ?? Docker Compose ??
spring.docker.compose.enabled=false

